.venv/
node_modules/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.env
experiments/**/output/
.idea/
*.stackdump

# Publish README.md and one canonical release-status document. Keep detailed
# sprint notes and other working material local.
docs/*
# Keep the 12 frozen arXiv HTML fixtures tracked. They are the reproducible
# corpus CI ingests, so everything else under data/ stays local.
data/*
!data/raw/
data/raw/*
!data/raw/sampled_html/
experiments/
*.md
!README.md
!docs/release-status.md
!tests/data/
# The evals' answer key ships with the evals that read it.
!evals/dataset/
# The published benchmark page ships; `docs/` above would otherwise catch it.
!evals/docs/
chroma_db/
