---
chat:
  base_url: https://vllm.trueworks.in/v1
  model: gemma4-summarizer
  api_key: unused
  temperature: 0

embeddings:
  base_url: https://ollama.trueworks.in
  model: qwen3-embedding:4b


evals:
  upload_results: false
  # Eval processes always overlay these values onto the application runtime.
  runtime:
    html_corpus: SAMPLE
    vector_store: CHROMA


runtime:
  html_corpus: SAMPLE # PROD or SAMPLE
  vector_store: CHROMA # WEAVIATE or CHROMA
